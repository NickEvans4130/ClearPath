*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# App build output
app/build/

# Downloaded tile files (large binaries, regenerate with scripts/download_tiles.sh)
app/src/main/assets/tiles/*.mbtiles
tiles/*.mbtiles

# NOTE: Fonts, gradle wrapper, and gradlew are committed for reproducible builds
